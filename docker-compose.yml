version: '2'
services:
  datalab-nlp:
    image: $IMAGE_NAME
    build:
      context: .
      dockerfile: Dockerfile-extended.in
    entrypoint: /datalab/run.sh
    ports:
      - 8081:8080
    volumes:
      - $HOME/datalab:/content/datalab
    environment:
      - PROJECT_ID=$PROJECT_ID
      - DATALAB_ENV=local
